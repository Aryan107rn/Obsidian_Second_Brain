---
tags: [javascript, moc, web-development, computer-science, placement-prep]
aliases: [JS MOC, JavaScript Index, JavaScript Revision Notes]
created: 2026-08-09
---

# JavaScript MOC (Map of Content)

Complete placement-ready JavaScript revision path — fundamentals → async → ES6+ → DOM → interview drill sheet. Work through in order for first-time learning, or jump directly to a topic for revision.

## 1. Fundamentals
- [[Variables and Data Types]] — `var`/`let`/`const`, primitives vs reference types, `typeof`
- [[Operators and Type Coercion]] — `==` vs `===`, truthy/falsy, `??` vs `||`, coercion table

## 2. Scope, Closures & Functions
- [[Scope and Hoisting]] — execution context, hoisting, Temporal Dead Zone
- [[Closures]] — the `var`-in-loop bug, data privacy, function factories
- [[this Keyword]] — 4 binding rules, `call`/`apply`/`bind`, arrow functions
- [[Functions in JavaScript]] — declarations vs expressions vs arrows, IIFE, HOFs, pure functions

## 3. Objects & OOP
- [[Objects Destructuring Spread Rest]] — destructuring, spread/rest, shallow copy gotchas
- [[Prototypes and Inheritance]] — prototype chain, ES6 classes, getters/setters, private fields

## 4. Collections
- [[Array Methods]] — map/filter/reduce, mutating vs non-mutating, sort gotchas, polyfills
- [[ES6+ Modern Features]] — Map/Set/WeakMap, template literals, optional chaining, generators, Symbol/BigInt

## 5. Asynchronous JavaScript
- [[Asynchronous JavaScript]] — callbacks, Promises, combinators, async/await, sequential vs parallel
- [[Event Loop]] — call stack, microtask/macrotask queues, worked trace example (with diagram)

## 6. Browser & DOM
- [[DOM and Events]] — selecting/modifying elements, bubbling vs capturing, event delegation

## 7. Patterns, Errors & Memory
- [[Common Coding Patterns]] — currying, debounce, throttle, memoization
- [[Error Handling and Memory]] — try/catch/custom errors, modules, JSON, garbage collection, storage APIs

## 8. Interview Prep
- [[JS Interview Questions and Tricky Outputs]] — ranked Q&A drill sheet + predict-the-output practice

## How to Use This MOC

1. **First pass:** read sections 1–7 in order — each builds on the last (e.g. closures need scope, `this` needs functions).
2. **Revision pass:** jump straight to any note; each is self-contained with its own examples and a "Key Takeaways" section.
3. **Pre-interview:** run through [[JS Interview Questions and Tricky Outputs]] alone as a rapid-recall check — it links back to every deep-dive note for anything you can't answer confidently.

## Related Areas
- [[REST APIs]] — how JS (fetch/async-await) talks to servers
- [[Git and GitHub]] — version control for JS projects
